overwrite: true
schema: "src/**/*.graphql" # Path to your GraphQL schema
generates:
  ./types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: ./context#Context # Path to your context type